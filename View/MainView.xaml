<UserControl x:Class="SmartTime.View.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartTime.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Label Name="NowWorkTimeLabel" Content="{Binding Path=NowWorkTimeString,UpdateSourceTrigger=PropertyChanged}"  HorizontalAlignment="Left" Margin="10,35,0,0" VerticalAlignment="Top" Height="80" Width="455" Background="Transparent" FontSize="48" VerticalContentAlignment="Center" FontWeight="Black" Foreground="#FF2C3E50"/>
        <Label Name="NeedTimeToWorkLabel" Content="{Binding Path=NeedWorkTimeString,UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" Margin="10,115,0,0" VerticalAlignment="Top" Height="35" Width="365" Background="Transparent" FontSize="20" VerticalContentAlignment="Center" FontWeight="Bold" Foreground="#FF2C3E50"/>
        <Button Content="⟳" HorizontalAlignment="Left" Margin="450,25,0,0" VerticalAlignment="Top" Width="20" Height="20" Click="UpdateStats" BorderBrush="#FF2980B9" Foreground="#FF2C3E50" Background="#FF3498DB"/>

    </Grid>
</UserControl>
